
var method = Pod.prototype;

function Pod(type,token) {
    this._type = type;
    this.token = token;
}

method.createFolder = function(locationPath) {
   
};

method.createFile = function(locationPath,data) {
  
};

module.exports = Pod;